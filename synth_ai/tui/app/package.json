{
  "name": "synth-ai-tui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run typecheck && bun build src/index.ts --outdir dist --bundle --target bun --entry-naming=[name].mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@opentui/core": "^0.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
