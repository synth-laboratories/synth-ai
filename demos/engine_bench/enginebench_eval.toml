# Simple Eval - Run 1 rollout on EngineBench
[eval]
localapi_url = "http://localhost:8020"
seeds = [0]
num_workers = 1

[eval.policy]
model = "gpt-4o-mini"
timeout = 180

[eval.policy.config]
model = "gpt-4o-mini"
timeout = 180
