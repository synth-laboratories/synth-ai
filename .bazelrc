# Bazel cache config (local + Blacksmith sticky disk)
common --color=yes
common --keep_going
common --disk_cache=~/.cache/bazel-disk
common --repository_cache=~/.cache/bazel-repo
common --enable_bzlmod
common --action_env=PATH
common --action_env=HOME
common --action_env=UV_CACHE_DIR
