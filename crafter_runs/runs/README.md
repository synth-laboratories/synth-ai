# Crafter run artifacts

Each `run_crafter_deo_hillclimb_1cand_slot1.sh` execution writes a timestamped
folder here (`summary.json`, `driver.log`, `workspace.tar.gz`, etc.) unless
`OUTPUT_ROOT` points elsewhere.
