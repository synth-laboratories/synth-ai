//! Environment Pools client (thin wrapper).
//!
//! The core request semantics live in `synth-ai-core`. This module exists to keep the
//! `synth-ai` crate's public API stable.

pub use synth_ai_core::EnvironmentPoolsClient;

